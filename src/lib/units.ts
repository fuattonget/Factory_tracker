// Shared unit-conversion constants -- previously duplicated independently
// in dashboard.ts and pipeOverview.ts (and needed again by pipes.ts for the
// repair-ratio formula), factored out to one place.
export const METERS_PER_FOOT = 0.3048;
export const FEET_PER_METER = 1 / METERS_PER_FOOT;
